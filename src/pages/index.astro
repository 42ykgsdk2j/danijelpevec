---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import WhoSection from "../components/WhoSection.astro";
import ChallengesSection from "../components/ChallengesSection.astro";
import AssessmentCtaSection from "../components/AssessmentCtaSection.astro";
import ApproachSection from "../components/ApproachSection.astro";
import WorkSection from "../components/WorkSection.astro";
import BlogSection from "../components/BlogSection.astro";
import AboutSection from "../components/AboutSection.astro";
import FinalCta from "../components/FinalCta.astro";
import { SITE } from "../lib/i18n";
import { personJsonLd, businessJsonLd, websiteJsonLd } from "../lib/seo";

const lang = "hr" as const;
---
<BaseLayout
  lang={lang}
  title="Danijel Pevec — Savjetnik za obiteljski biznis"
  description="Pomažem osnivačima i njihovim obiteljima izgraditi nasljeđe kroz odluke koje oblikuju rast, sukcesiju i tranziciju među generacijama."
  canonical={`${SITE}/`}
  hreflangEn={`${SITE}/en/`}
  hreflangHr={`${SITE}/`}
  jsonLd={[personJsonLd(lang), businessJsonLd(lang), websiteJsonLd(lang)]}
>
  <Hero lang={lang} />
  <WhoSection lang={lang} />
  <ChallengesSection lang={lang} />
  <ApproachSection lang={lang} />
  <WorkSection lang={lang} />
  <AssessmentCtaSection lang={lang} />
  <BlogSection lang={lang} />
  <AboutSection lang={lang} />
  <FinalCta lang={lang} />
</BaseLayout>
